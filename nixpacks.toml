# Configuração Railway - Deploy Unificado (Frontend + Backend)
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["bash install-all.sh"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node backend/src/index.js"
