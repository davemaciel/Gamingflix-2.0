# Configuração Railway - Deploy Unificado (Frontend + Backend)
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "npm run build",
  "npm install --prefix backend"
]

[start]
cmd = "npm start"
